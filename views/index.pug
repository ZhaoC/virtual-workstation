extends layout

block content
  .container
    h1 Welcome... 
    br 
    if (!user)
      a(href="/login") 
        p Login
      br
      a(href="/register") 
        p Register
    if (user)
      p You are currently logged in as #{user.username}
      a(href="/logout") Logout
